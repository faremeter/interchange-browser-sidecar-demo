{
  "name": "interchange-browser-sidecar-demo",
  "version": "0.1.0",
  "license": "LGPL-2.1-only",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:interchange": "bun run src/link-interchange.ts",
    "start": "bun run prepare:interchange && bun --conditions=intx-src run src/server.ts",
    "test": "bun --conditions=intx-src test",
    "typecheck": "bun run prepare:interchange && bunx tsc --noEmit"
  },
  "dependencies": {
    "arktype": "2.1.29",
    "isomorphic-git": "^1.27.2"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
